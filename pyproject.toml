[project]
name = "legal-review-env"
version = "1.0.0"
description = "Legal contract review OpenEnv environment"
authors = [{name = "jwell123"}]
dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"